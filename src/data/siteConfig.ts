export interface SiteConfig {
  brandName: string;
  logoPath: string;
  logoWidth: number;
}

export const siteConfig: SiteConfig = {
  brandName: 'Nakama Digital',
  logoPath: 'uploads/logo/logo.svg',
  logoWidth: 150,
};
